name: grid
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8.8
  - pip
  - requests
  - ipykernel
  - ipywidgets
  - pip:
    - torch
    - torchvision
    - argparse
    - datetime
    - pathlib
    - numpy
    - matplotlib
    - pycocotools
    - scikit-image
    - pandas
    - seaborn
    - pillow